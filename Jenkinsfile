pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code checked out from GitHub'
            }
        }

        stage('Hello') {
            steps {
                echo 'Hello from Jenkins auto build!'
            }
        }
    }
}
